version: '3'
services:
  ffa-eval:
    build: .
    image: ffa-nettest:latest
    working_dir: /workspace/FFA-Nettest/net
    volumes:
      - /data2/hyz/FFA-Nettest:/workspace/FFA-Nettest
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
